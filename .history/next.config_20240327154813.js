import { useRouter } from 'next/compat/router'
/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options */ 
}
const withBlitz = require('@blitzjs/next');
module
 .exports = withBlitz(nextConfig);
 // Only uncomment the lines below if you
 // have previously set up the Blitz app
 // with `blitz install` or `blitz new`
 // const withBlitz = require
